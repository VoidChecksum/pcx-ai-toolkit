# PCX Toolkit Improvement Series Design

Goal: implement the improvement series as reviewable PR-scoped changes.

PRs: MCP schema mirror, scenario fixtures, Rust validators, native MCP read-only commands, offline doc drift, RE exporters, distribution docs, safety metadata, README denylist.
